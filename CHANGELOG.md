# История версий NovelMaestro

## 0.2.10

### Новое

- **Версия сборки в интерфейсе** — новый модуль `web/version.py` + файл
  `VERSION` в корне репо (приоритет: NOVELMAESTRO_VERSION → VERSION →
  фолбэк); версия показывается внизу сайдбара SPA, отдаётся в
  `/api/session` и в HTTP-заголовке `Server: NovelMaestro/<версия>`;
  обновляется при каждом релизе вместе с этим файлом.

### Исправления

- **Windows: кириллица в логах Запусков — чёрные ромбы** — потомки
  web-сервера писали в stdout-пайп в локальной кодировке (cp1251), а
  читались как utf-8 → U+FFFD; символы прогресса (✦/📊) в cp1251
  непредставимы. Теперь stdout потомков — строго utf-8
  (PYTHONIOENCODING + encoding в Popen).

## 0.2.9

### Новое

- **Оценка перевода (LLM): фиксированный отчёт** — имя
  `tmp/translation_quality_assessment.md` зафиксировано в web:
  поле «Выходной файл» из формы Запусков убрано (в CLI `--output`
  сохранён); вкладка «Проверки» → «Оценка перевода (LLM)» рендерит
  отчёт без выбора файла; при отсутствии отчёта — подсказка запустить
  стадию.

### Исправления

- **Бюджет «Оценки перевода» урезался ~вдвое** — дерево глав
  (`/api/.../tree`) отдавало размеры артефактов в БАЙТАХ, а расчёт
  «Конечной главы» ведётся в СИМВОЛАХ: utf-8 кириллицы — 2 байта/сим.,
  CJK — до 3. Теперь сервер декодирует utf-8 и отдаёт символы —
  фактический размер запроса совпадает с предпросмотром (при бюджете
  200 000 симв. влезает ~200 000, а не ~110 000).
- **Проверка глоссария: RAG-поля не уезжают в другие режимы** —
  форма помнит значения прошлого RAG-запуска, и они попадали в argv
  при любом режиме «Режимов» (`--rag_terms`, `--rag_source_type`,
  `--rag_budget`, `--save-interval` при «Выбранных типах»). Теперь
  RAG-поля передаются только в режиме RAG; в CLI — предупреждение
  при `--rag_terms` вне режима RAG.

## 0.2.8

### Новое

- **Разметка промптов тегами `<system>`/`<user>`** — содержимое
  `<system>…</system>` уходит в системное сообщение LLM, остальной
  текст (и `<user>…</user>`) — в пользовательское; без разметки весь
  промпт в user (как раньше). Работает во всех стадиях и в
  «Предпросмотре запроса» (единый `llm_messages`); встроенные
  системные промпты и шаблоны General переведены на разметку.
- **Промпты: только рабочие теги** — из встроенных промптов и
  `pipeline_prompt.txt` убрана фиктивная xml-разметка данных
  (`<dictionary>`, `<source_text>`, `<draft_translation>` и т.п. —
  программой не обрабатывалась): блоки размечены текстовыми
  маркерами «=== СЛОВАРЬ ===», «=== ОРИГИНАЛ ===» и т.п.
- **Компиляция: разбивка на части для любого режима** — поле
  «Глав в части» работает всегда (>0 — диапазон режется на части,
  пусто/0 — одна сборка); режимы «* частями» из меню убраны.
- **Компиляция: именование с названием проекта** — все файлы сборки
  `{Имя_проекта}_{начало}_{конец}…` вместо `compiled_*` (например
  `Becoming_a_Sage_Starting_from_the_Time_Dao_Fruit_1_433.txt`);
  запись атомарная.
- **Проверки: массовые кнопки в сводке** — справа над списком правок:
  «Принять все» / «Отклонить все» / «Применить все принятые» /
  «Очистить» (перенесена с низа карточки).
- **Причины пропуска правок — note в review** — неприменимые правки
  (текст главы менялся после проверки и т.п.) получают поле `note` с
  причиной; видно в карточке «Проверки»; успешное применение снимает
  note.

### Исправления

- **Проверка перевода (LLM): дрейф атрибуции ±1** — цитата теперь
  сверяется с текстом батча всегда (раньше главы из батча принимались
  вслепую); при применении непопавшие правки перепривязываются к
  реальной главе — поиск по ВСЕЙ книге, применение только при РОВНО
  одном совпадении ровно в одной главе (короткие цитаты не ищутся,
  неоднозначные остаются с note); типографика («»/—/…/...") не
  считается расхождением; dry-run откатывает и применение, и
  перепривязку.
- **Рабочие файлы — в `tmp/` проекта** — review-файлы
  (`tmp/ner_review.json`, `tmp/translate_check_llm_review.json`),
  бэкап глоссария (`tmp/ner.json.bak`), отчёт оценки
  (`tmp/translation_quality_assessment.md`) и все сборки compile не
  засоряют корень проекта; старые файлы из корня переносите в `tmp/`
  вручную (fallback-чтения старых путей нет).

## 0.2.7

### Изменения

- **Единый формат LLM-запросов** — новый сборщик сообщений
  `llm_messages()` в `core/common.py`: промпт стадии уходит в роль
  USER, SYSTEM остаётся в запросе пустым (`content: ""`); все
  LLM-скрипты (translate_book, ner, ner_check,
  translate_check_llm, translate_quality, wiki) переведены на один
  сборщик — фактический запрос и «Предпросмотр запроса» собираются
  одной функцией, в предпросмотре видны обе роли.
- **Запуски: живые счётчики у чипсов типов** — числа в скобках
  пересчитываются по настройкам стадии: ner_check — по порогу
  «count» (0 = без порога), wiki — по «Мин. частоте» и непустому
  переводу; в wiki чипсы показывают базовые типы (Person (male) →
  Person и т.п.) — как их фильтрует CLI; инфострока единая:
  сколько терминов попадёт в запрос/проверку, пусто = все типы.

### Исправления

- **compile: разделители `* * *` в EPUB/FB2** — убрано экранирование
  `\* \* \*` (остаток pandoc-эпохи): разделители сцен снова
  рендерятся в книгах; в FB2 — центрированный разделитель, как
  в EPUB.
- **Запуски: журнал следует за проектом** — перенос в другой
  раздел или переименование больше не «теряет» логи стадий:
  после успешной операции project/cwd у записей журнала
  переписываются (`JobManager.update_project_path`).

## 0.2.6

### Новое

- **Перевод с расширенным контекстом** (малоресурсные языки):
  словарь (`dict.json`), правила языка (`rules.txt|md`) и пары
  оригинал→перевод (`examples.json`) из `source/` попадают в промпт
  через `{dict_block}/{rules_block}/{fewshot_block}`; автодетект
  направления словаря и примеров (по стороне с большим совпадением);
  общий бюджет запроса `--request_budget` (СИМВОЛЫ) с превышением =
  FAIL чанка; действие 9 в конвейере и тег `<translate_lr>`
  (фолбэк `<translate>` → встроенный промпт).
- **ner_check: RAG-проверка термина** — новый режим `rag`:
  запрос по ОДНОМУ термину с JSON-записью глоссария ({ner_block})
  и фрагментами книги ({rag_block}); тег промпта `<prompt_rag>`,
  общий `<prompt_check>` → `<prompt_ner_check>`; кнопка «Добавить
  спорные» подтягивает термины с конфликтными вариантами из
  полей `_votes_*`.
- **Запуски: таблица глав под логом конвейера** — «Главы · перевод
  → редактура → полировка» как интерактивная визуализация лога:
  строки только заданного диапазона (пустой = все), клетки
  ✓/✗/⊘ обновляются живьём из SSE; для завершённого запуска —
  из истории.

### Изменения

- **Запуски: упрощение форм** — глоссарий всегда `ner.json`
  (выбор файла убран из всех стадий); ner без «Режима» и входного
  txt (нет файла — новый, есть — дообучение; вход — сборка глав
  из диапазона); review-файлы без выбора (стандартные имена,
  их же читают вкладки «Правки»); единый «Таймаут, сек»
  (stream_timeout передаётся тем же числом); убрано действие 10,
  поля словаря dict_fields, бюджеты правил/примеров (общий
  --request_budget).
- **Вставки промптов в JSON** — {ner_block}, {glossary},
  {fewshot_block}, RAG-блок — JSON-записи/массивы по строке;
  экспорт ner format=text → JSONL.
- **min_len_ratio отключён по умолчанию** во всех режимах перевода:
  контроль соотношения длин давал ложные срабатывания; размер
  перевода проверяет стадия «Проверка перевода»; защита от пустого
  ответа LLM работает всегда.
- **Regexp-поля — чистый стандарт Python re** (MULTILINE), без
  кастомных флагов и комментариев; replacements.txt выпилен
  (правила — из формы/аргументов).

### Исправления

- **Извлечение тегов промптов** — ner.py, wiki.py,
  translate_check_llm.py имели собственные неякоренные regex:
  упоминание тега в «#»-комментарии захватило бы мусор в промпт;
  все скрипты переведены на единый get_tagged_prompt (открывающий
  тег — только в начале строки).
- **Автоподхват «Общего промпт-файла»** — кэш опций общий на проект,
  а auto_prompt считался только для pipeline: первая чужая стадия
  кэшировала прочерк; теперь auto_prompt считается для любой стадии.
- **Действие 9** — не превращается в полный цикл; выбор ner.json
  корректно уходит в оркестратор.
- **epub: срез комментария в --header-regexp** — хвостовые пробелы
  справки не ломали паттерн; справки regexp унифицированы.
- **Статусы глав заметнее** — отметки ✓/✗/⊘ крупнее и насыщеннее
  по темам (вкладка «Статус» и таблица под логом).

## 0.2.5

### Изменения

- **translate_check: флаг « |s» в regexp-проверках** — «пропустить
  первое совпадение»: правило `^\s*Глава\s+\d+ |s` ловит лишние
  заголовки «Глава N», не помечая сам заголовок главы; комментарий
  « # …» и флаг в одной строке; справка в форме стадии дополнена.

### Исправления

- **Запуски: живой лог и статусы** — SSE-стрим не подключался
  («Assignment to constant variable»), лог был пуст, статус
  (остановлен/готово) приходил только после перезагрузки страницы.
- **Предпросмотр запроса LLM** — модалка падала «can't convert null
  to object» на всех стадиях (null-атрибуты в h()).
- **Проверки: «Применить»** — кнопка запускала полный LLM-прогон
  вместо применения правок ner_review.json (флаги apply/dry_run/
  no_bak потеряны при рефакторинге build_ner_check); apply-флаги
  больше не пишутся в pdir/.env.
- **Запуски: чипсы полей/типов** — выбор в чипсах-чекбоксах
  (поля {ner_block}, типы глоссария) переживает перезагрузку
  страницы (localStorage per проект+стадия), дефолт помечается
  как выбранный явно — «видимое = отправляемое».
- **Конвейер: --no-aliases** — оркестратор передавал несуществующий
  флаг --no_aliases; при снятых алиасах запуск и предпросмотр
  падали на argparse стадии 1.

## 0.2.4

### Изменения

- **Запуски: лог последнего завершённого запуска остаётся на вкладке
  стадии** — панель лога показывается для текущего запуска стадии в
  любом статусе (после завершения остаётся с финальным статусом и
  временем) либо — последний завершённый запуск ЭТОЙ стадии этого
  проекта (лениво из истории запусков, кэш по стадии); лог всегда
  соответствует открытой вкладке, новый запуск переключает панель на
  активный (live-гард: строки чужого запуска не утекают в историю);
  `/api/jobs/{id}` отдаёт лог целиком (весь кольцевой буфер, не 500
  строк).
- **Конвейер: выбор полей терминов в `{ner_block}`** — чипсы-чекбоксы
  в обоих режимах формы (как «Поля в запросе LLM» в ner_check):
  базовые type/translation/aliases + ключи из реального ner.json
  проекта; выбор общий для всех 3 стадий конвейера (`--ner_fields`),
  снятие aliases отключает авто-добавление алиасов (`--no_aliases`).
- **ner: границы предложений любых языков и нечёткий контекст
  термина** — в поле context поглощаются закрывающие кавычки/скобки
  после знака конца предложения; знаки конца любых языков (арабский,
  урду, деванагари, армянский, эфиопский); фолбэк: нечёткий поиск
  термина по предложениям чанка (n-грамм-перекрытие + longest match,
  порог = `--threshold` стадии ner; CJK — только точный).
- **Предпросмотр запроса LLM** — кнопка (только Экспертный режим) у
  всех LLM-стадий (pipeline, ner, ner_check, translate_check_llm,
  translate_quality, wiki): эмуляция ПЕРВОГО запроса стадии без сети
  (чанк/батч/термин 1 — из того же кода сборки, что реальный запуск;
  артефакты и логи не создаются) + модалка с сообщениями system/user,
  статистикой символов и параметрами; API — `POST
  /api/stages/{key}/preview-request` (таймаут 300 с).

## 0.2.3

### Изменения

- **translate_check: настраиваемые структурные проверки** — минимальный
  размер файла — отдельное поле `--min-file-size` (БАЙТЫ, дефолт 3072);
  заголовок главы — отдельный regexp `--header-regexp` (пусто =
  «Глава N» без учёта регистра); «Проверять последовательность глав»
  — отключаемый чекбокс (`--no-sequence-check`, по умолчанию включён):
  первое число в первой непустой строке должно быть ровно на 1 больше
  предыдущего (N+1); лишние заголовки «Глава N» перенесены из
  структурной проверки в дефолтный набор regexp-проверок — первое
  совпадение (сам заголовок главы) не ошибка, последующие — «Лишний
  заголовок главы», паттерн наследует настраиваемый `--header-regexp`.

### Исправления

- **Вкладка «Главы»** — диапазон задаётся двумя полями (начало/конец,
  как в «Запусках»); строки идут в порядке номеров: существующие главы
  на своих местах, пропущенные — серыми ячейками «N —»; удаление
  файлов — модалка с подтверждением словом УДАЛИТЬ.
- **Форма «Оценка перевода»** — падение при неопределённом выбранном
  промпте: пересчёт конечной главы не вешается на отсутствующий
  элемент.
- **`templates/.env.example`** — комментарии вынесены отдельными
  строками (без inline после значений); дефолтные regexp-проверки —
  реальные паттерны.

## 0.2.2

### Изменения

- **Бюджет LLM-запросов — только содержимое** — длина промпта больше
  не вычитается из бюджета: `--budget`/`--rag_budget`/`--chunk_size`
  ограничивают только главы/фрагменты/пакеты (СИМВОЛЫ), промпт идёт
  сверх бюджета. Применено к «Оценке перевода» (CLI и web-калькулятор
  конечной главы) и RAG-режиму ner_check.
- **Конвейер: суммарные потоки** — поля «Параллельных глав» и «Потоков
  на главу» объединены в одно «Потоков (1–16)»: задаётся СУММАРНОЕ
  число одновременных LLM-запросов, распределение по главам и чанкам
  считает web-оркестратор (`--threads` убран).
- **ner_check · RAG: термин = запрос** — каждый термин проверяется
  отдельным LLM-запросом (раньше все термины уходили одним гигантским
  пакетом); запросы идут параллельно, «Параллельные пакеты» — число
  одновременных запросов; бюджет на термин — только фрагменты книги.
- **ner_check: теги промптов** — промпт «Выбранные типы» обёрнут
  в `<prompt_check>`, RAG — в `<prompt_rag>`; комментарии в
  промпт-файлах пишутся вне тегов строкой с `#` (единообразно с
  остальными промптами).
- **translate_check: проверки и форма** — ищутся ВСЕ иероглифы (все
  CJK-блоки, а не только `[一-鿿【】「」『』]`); regexp-проверки сканируют
  все строки включая заголовок главы; лишние заголовки «Глава N»
  находятся структурно; каждая найденная ошибка — отдельной строкой
  (клик по ошибке ищет одно вхождение, а не весь список). Поля
  переименованы: «Выбранная Стадия/Предыдущая Стадия (по занимаемому
  месту)» и «Выбранная Стадия/Оригинал (по занимаемому месту)»;
  дефолтные значения полей переехали в системный `.env`
  (`TRANSLATE_CHECK_NEIGHBOR`/`TRANSLATE_CHECK_ORIGINAL`/
  `TRANSLATE_CHECK_REGEXP_CHECKS`) — пустой `.env` = пустое поле;
  флаг `--lenient` из web убран.
- **Конфиг и Docker** — `templates/.env.example`: `HOST=` пусто
  (сервер задаётся пользователем), дефолты translate_check;
  docker-compose переопределяет их переменными окружения (включая
  `TZ` для местного времени); в образ добавлен `tzdata`.
- **Отчёт «Оценка перевода»: местное время** — дата отчёта пишется
  локальным временем (`datetime.now().astimezone()`), а не UTC.
- **Вкладка «Главы»** — диапазон глав одной строкой (как в «Запусках»,
  префилл — весь диапазон); кнопка «Удалить файлы» стирает файлы
  выбранного типа в диапазоне (с подтверждением); пропущенные главы
  показываются серыми ячейками «N —» (корректно в светлой и тёмной
  темах). API: `GET …/chapters/titles` отдаёт `all_ids`/`missing`,
  добавлен `DELETE …/chapters`.

## 0.2.1

### Исправления

- **Отчёт «Оценка перевода» не показывался** — внутренний
  рендерер файла затенял имя проекта: запрос к `/api/file` уходил
  с `project=ACTIVE/translation_quality_assessment.md` и падал с 404;
  ошибка рендера не вставлялась в контейнер и оставалась невидимой.
  Параметр переименован, ошибка выводится.
- **«Оценка перевода»: Экспертный режим** — «Конечная глава» теперь
  редактируема: ручной ввод проверяется по бюджету запроса и
  обрезается до последней влезающей главы с подсказкой; простой
  режим — авторасчёт как раньше. Починено чтение бюджета в
  экспертной форме (поле-обёртка вместо значения).
- **Промпт оценки перевода** — плейсхолдеры `{original_text}` /
  `{translated_text}` были только в комментариях шаблона, тело тега
  `<prompt_assessment>` их не содержало (текст не подставлялся);
  вставлены в тело.
- **translate_check: форма запуска** — типы файлов глав без
  уточнений в скобках (просто `polished`, `redacted`, …); поля
  «Допуск по длине»/«Допуск по оригиналу»/regexp-проверки
  предзаполнены дефолтами скрипта (в Docker поле regexp не было
  пустым — дефолтные проверки перенесены из CLI в форму);
  раскрывающаяся справка по regexp-проверкам; чекбокс
  «Мягкий режим (--lenient)» убран из web (CLI сохраняет флаг).
- **ner_check RAG искал по переводу** — FTS5-поиск шёл по полю
  `translation`, из-за чего в китайском тексте глав не находилось
  ничего («✔ Определено: 0 терминов»); теперь поиск по `term`
  (с фолбэком на перевод). Корень: токенизатор unicode61
  склеивает сплошную CJK-строку в один токен — иероглифы
  разбиваются пробелами в поисковом индексе, фрагменты выдаются
  оригинальным текстом. В Экспертном режиме скрыты поля
  «Бюджет пакета»/«Порог count»/«Потоки» (не используются в RAG).
- **«Проверка перевода»: кнопка «Очистить»** — удаление всех
  отчётов `logs/check_*.txt` с подтверждением «УДАЛИТЬ» и пустым
  состоянием после очистки.

## 0.2.0

### Новое

- **Стадия «Оценка перевода (LLM)»** — `translate_quality` между
  «Массовые замены» и «Компиляция TXT/EPUB/FB2»: один LLM-запрос по
  пакету глав диапазона; `{original_text}` (chapter.txt) и
  `{translated_text}` (тип файлов глав) подставляются в промпт
  (тег `<prompt_assessment>`, шаблон в `templates/General/prompts/`).
- **Простой/Экспертный режимы** — простой: диапазон глав, тип
  файлов, промпт-файл; экспертный: + стандартные LLM-поля (сервер,
  модель, API-ключ, таймаут, повторы, температура, reasoning
  effort) и бюджет запроса в СИМВОЛАХ (главы + промпт); если главы
  не влезают — пакет обрезается до целого количества глав, отсечённые
  указываются в отчёте.
- **md-отчёт оценки** — `translation_quality_assessment.md`
  (настраиваемый выход): техническая шапка (дата, диапазон, пакет,
  бюджет, модель) + оценка LLM; вкладка «Проверки» → «Оценка
  перевода (LLM)» рендерит отчёт (выбор md-файла, дефолт
  `translation_quality_assessment.md`).
- **compile · «Тип файлов глав»** — поле `source_type` переименовано
  (было «Исходный файл главы»).

### Исправления

- **«Оценка перевода»: конечная глава по бюджету** — в форме
  задаётся только начало диапазона, «Конечная глава» рассчитывается
  интерактивно по бюджету запроса (главы + промпт) и обновляется при
  изменении начала/бюджета/типа файлов/промпта; больше последней
  главы книги не бывает.
- **ner_check RAG: поля записи в промпте** — выбранные чипсами
  «Поля в запросе LLM» (термин — всегда) уходят в RAG-промпт
  вместе с релевантными фрагментами книги; чипсы полей остаются
  видимыми в RAG-режиме.
- **Справка актуализирована** — «Оценка перевода (LLM)», RAG-поля,
  единая строка диапазона, «Тип файлов глав» у compile, wiki
  «Собрать из глав» по умолчанию.
- **Тест вкладок проекта** — `tests/spa/tabs.test.mjs` прогоняет все
  вкладки с DOM-mock и ловит ReferenceError-класс регрессов;
  `tests/spa/quality.test.mjs` — расчёт конечной главы по бюджету.

- **Вкладка «Проверки» не открывалась** — `renderQualityReports`
  (секция «Оценка перевода») обращалась к `section`/`name` из
  замыкания viewProject, будучи глобальной (ReferenceError); теперь
  оба рендерера принимают проект параметрами. Добавлен
  node-тест `tests/spa/tabs.test.mjs`: все вкладки проекта
  прогоняются с DOM-mock — регрессы «пустая вкладка» ловятся
  заранее.
- **Запуски · дубль диапазона глав** — в простом режиме
  translate_check/translate_quality поля «Начальная/Конечная глава»
  больше не рисуются отдельно: осталась единая строка
  «Главы: [start] – [end]».
- **wiki · «Источник текста»** — по умолчанию «Собрать из глав»
  (склейка chapters/* в память).
- **Кнопки «+»/«x» глоссария** — стиль `btn-ghost`, как остальные
  кнопки панели (в светлой теме не темнее).

### Новое · translate_check

- **translate_check · «Тип файлов глав» вместо пресета** — выбор
  типа (polished/redacted/translated) определяет сравнения:
  polished → redacted+chapter, redacted → translated+chapter,
  translated → только chapter; простой режим с карточкой
  «Проверить перевод».
- **translate_check · «ratio±tol» в одном поле** — «Стадия/Стадия
  (по занимаемому месту)» и «Оригинал/Стадия (по занимаемому
  месту)» вместо четырёх полей ratio/tol; формат `1.0±0.05`
  (`--neighbor`/`--original`).
- **translate_check · универсальные regexp-проверки** — чекбоксы
  отключения и «свои форматы» убраны: каждая строка — regexp по
  тексту главы (multiline), всё найденное — ошибка, первое
  вхождение на заголовке главы не считается; по умолчанию —
  иероглифы, латиница, лишние «Глава N».
- **Комментарии « # …» в regexp-полях** — паттерны разбивки epub,
  regexp-проверки translate_check и правила batch_replace
  поддерживают комментарий в конце строки; многострочные поля
  предзаполняются из `.env` одной строкой с переносами как «\n»
  (`EPUB_SPLIT_PATTERNS`, `TRANSLATE_CHECK_REGEXP_CHECKS`,
  `BATCH_REPLACE_REPLACEMENTS`).
- **Предпросмотры при светлой теме** — явный цвет текста у
  предпросмотра замен, epub-разбивки и compile: чёрные буквы на
  тёмном фоне невозможны при любой теме.

### Изменения

- `cli/translate_check.py`: `--preset` → `--check-type`,
  `--ratio-*/--tol-*` → `--neighbor`/`--original`,
  `--no-nonrussian`/`--nonrussian-regex`/`--no-chapter-order`/
  `--chapter-regex` → `--regexp-check` (повторяемый).
- `core/common.py`: новая функция `strip_line_comment` (inline-
  комментарий « # …» в строке правила).

## 0.1.12

### Новое

- **ner_check · потоки на батчи/типы** — все батчи всех проходов
  выполняются параллельно (`--threads`, 1–16) с общим
  прогрессбаром (done/total по всем типам × чанкам, не текущий
  чанк).
- **wiki · настройки поиска в промпте** — ключевые слова, русские
  имена типов, подписи отношений, порядок типов и пропускаемые
  отношения вынесены из кода в теги `<wiki_markers>`,
  `<wiki_type_names_ru>`, `<wiki_relations_labels>`,
  `<wiki_skip_relations>`, `<wiki_type_order>` в `wiki_prompt.txt`.
- **ner_check · RAG-режим** — точечная проверка списка терминов:
  FTS5 собирает релевантные фрагменты книги равномерно по всей
  книге (не с начала) в бюджет `rag_budget`; LLM уточняет
  тип/пол и перевод; отличия от ner.json — патчи stage=RAG в
  review-файл; промпт — тег `<prompt_rag>`.
- **ner.json · count опционален** — порог 0 выключает фильтр
  (записи без count не выпадают), строковые count нормализуются
  во всех сортировках/порогах.
- **translate_check · настройки** — коэффициенты пресетов
  (`--ratio-neighbor`/`--tol-neighbor`/`--ratio-original`/
  `--tol-original`, эвристики, подбираются опытным путём);
  отключаемая проверка не-русских символов (`--no-nonrussian`,
  свой `--nonrussian-regex`); отключаемая проверка
  последовательности глав (`--no-chapter-order`, свой
  `--chapter-regex`).
- **compile · очистка текста** — удаление «Глава N» из тела главы
  (после 9-й строки) по умолчанию включено; отключается
  `--no-clean`, формат — `--clean-regex`.
- **FTS5-утилиты в core.common** — `build_fts_index`,
  `fts_escape`, `fts_search_all/first/ids_all`, `even_sample`
  перенесены из wiki.py (правило слоёв).

### Исправления

- **Пустая вкладка «Глоссарий»** — ReferenceError: `dispute`/
  `voteKeys`/`disputeVictims`/`saveDispute` использовались без
  объявлений; добавлены объявления + localStorage-состояние
  фильтра «Спорные» и регрессионный тест.

## 0.1.11

### Новое

- **Pipeline · пороги count для глоссария и имён** — термины с
  `count` ниже порога не попадают в промпт: для `{ner_block}`
  (по умолчанию 0) и для `{female_names}`/`{male_names}`
  (по умолчанию 10); настройка в форме стадии (Экспертный режим)
  и флагами `--ner_min_count`/`--names_min_count`.
- **Pipeline · общий прогресс по главам** — прогрессбар считает
  завершённые стадии (главы × стадии), а не чанки текущей главы.
- **Валидация потоков** — минимальное/максимальное число потоков
  (1–16) во всех стадиях с потоками: понятное сообщение в форме
  до запуска и 400 от сервера.
- **Глоссарий: группировка кнопок** — «+ Столбец»/«+ Термин» —
  одна кнопка «+», «− Столбец»/«✕ По фильтру» — одна «✕».
- **Глоссарий: фильтр «Спорные»** — термины, где максимум голосов
  в `_votes_*` делится на второй по величине меньше заданного
  коэффициента (по умолчанию 1.16); поле и коэффициент
  настраиваются в модалке, сохраняются в localStorage.
- **Docker: compose v2 + entrypoint** — права на каталоги данных
  чинятся внутри контейнера (chown + gosu), шаблоны копируются
  из снимка образа при пустом `./templates`.
- **Docker: конфиг через переменные окружения** — корневой `.env`
  наружу не пробрасывается; в образе копия `.env.example`
  (значения по умолчанию), реальные `HOST/API_KEY/MODEL` —
  закомментированные `environment` в compose; `env/.env` на проде
  читается через `env_file`.
- **Внешний вид — localStorage** — тема интерфейса/редакторов и
  кегль хранятся в браузере, а не в `.env` (12-factor); окно
  редактирования системного `.env` из «Настроек» убрано
  (`/api/settings`, `/api/env/template` удалены).
- **`{original_text}` в polish-шаблоне** — конец тега `<polish>`.

### Исправления

- **ner.json: count-строки** — `count` вида `"5"` (строка) ломал
  сортировку имён и пороги (`TypeError: bad operand type for unary
  -`); нормализация во всех загрузчиках (`_int_count`), защита
  потребителей — артефакты `[FAIL: …]` в polished.txt больше не
  появляются.
- **Отчёт ner_changes.md удалён** — проверка глоссария больше не
  создаёт `ner_changes.md`.
- **compile: предпросмотр скрыт в TXT-режимах** — обложка/файлы
  не показываются, когда компиляция идёт в TXT.

## 0.1.10

### Новое

- **«Глоссарий»: таблица по ключам JSON** — заголовки столбцов —
  сырые ключи ner.json (term, type, translation, …), без перевода;
  новый термин добавляется сверху (термин и перевод обязательны);
  «+ Столбец»/«− Столбец» добавляют/удаляют поле у всех терминов;
  «✕ По фильтру» удаляет термины по условию на любом поле: «=» —
  содержит, «≠» — не содержит (по подстроке, не точное совпадение),
  «>»/«<» — больше/меньше.
- **«Проверки»: пагинация списка правок** — 100 правок на страницу
  с пейджером и общим счётчиком, для всех видов правок
  (ner_review.json и translate_check_llm_review.json); вкладка
  «Проверка» переименована в «Проверки».
- **«Проверка глоссария (LLM)»: чипсы по умолчанию** — .env больше
  не подмешивает сохранённые типы/поля (раньше из `NER_CHECK_TYPES`
  могли «всплывать» Artefact/Race); по умолчанию выбраны ВСЕ типы,
  поля — term (всегда) + type/translation/notes/context, если они
  есть в данных ner.json.
- **Подтверждение «УДАЛИТЬ» для всех удалений и очисток** — ввод
  слова «УДАЛИТЬ» теперь требуется в «Правках» (правка, очистка
  файла), «Глоссарии» (столбец, удаление по фильтру), «Логах»
  (очистить все), истории запусков на Дашборде и удалении раздела.

### Исправления

- **«Открыть» в списке проектов** — кнопка оформлена как «▶ Запуски»
  и «Управление» (btn-ghost), а не как основное действие.
- **Убрана постобработка ner.json (без LLM)** — режим
  `--strip-meta`/`--min-count` удалён из `cli/ner.py` и формы
  «Создание глоссария» (поля «Удалить метаданные»/«Порог count»).

### Документация

- `AGENTS.md`: запрет тестов и правок на боевых проектах
  (`projects/`), раздел `projects/TMP` — песочница для
  экспериментов на копиях книг.

### Тесты

- 998 тестов (глоссарий: ключи-заголовки, добавление термина,
  столбцы, удаление по фильтру; пагинация правок; чипсы ner_check;
  удаление постобработки).

## 0.1.9

### Новое

- **«Массовые замены»: предпросмотр по главам** — вместо чекбокса
  «Предпросмотр (--dry-run)» (который ничего не показывал) панель
  как в разбивке epub: тип файлов глав, любая глава из диапазона
  «Главы: N–M» и подсветка итогового текста — красным удалённое,
  зелёным вставленное, со счётчиками по правилам. Правила
  применяются тем же путём, что реальный запуск; файлы не
  изменяются; пустые правила показывают главу без изменений.
- **«Проверка глоссария (LLM)»: поля записи — ключи из ner.json** —
  чипсы «Поля в запросе LLM» названы английскими ключами как в данных
  (term, type, translation, …); «term» — всегда в запросе и
  неотключаем; появилось в ner.json новое поле — оно само добавляется
  в выбор и уходит в LLM-промпт.

### Исправления

- **«Массовые замены»: диапазон глав в предпросмотре** — смена
  «Главы: N–M» сразу пересобирала список глав только после
  переоткрытия формы; исправлена ошибка «ft is not defined».
- **«Проверка глоссария (LLM)»: убрано «Предпросмотр (--dry-run)»** —
  в web не нужен (текст промпта в stdout не выводится), предпросмотр
  правок — во вкладке «Проверки» проекта.
- **epub «Разбор исходника»: убрано «Замены и очистки»** — поля
  дублировали «Массовые замены», замены до разбивки больше не
  передаются стадии. (CLI-флаг `--replace-re` остаётся — для ручных
  запусков.)

## 0.1.8

### Исправления

- **«Создание глоссария»: убрано возобновление с места остановки,
  повторы для неверного формата**: запуск продолжал перевод с кэша
  прошлого чанка — на большой книге глоссарий мог собраться не с
  начала, а при «Invalid NER format» повторных попыток не было вовсе.
  Теперь каждый запуск всегда начинает с первого чанка (все режимы,
  «Новый глоссарий» и «Дообучение»), `--retries` — единый бюджет
  попыток и для сетевых сбоев, и для невалидного формата ответа,
  разбор JSON стал терпимее к хвостам текста, устаревшие файлы
  прогресса/кэшей удаляются.
- **«Проверка глоссария (LLM)» — поля записи вместо флагов**: убраны
  «Исключить слова», «Показывать алиасы» и «Показывать голоса»;
  «Таймаут стрима» объединён с общим таймаутом. В LLM отправляются
  только выбранные поля (термин — всегда), список полей собирается из
  реального ner.json и выбирается чипсами. Заодно починен баг «по
  типам»: выбранные типы не попадали в параметры запуска.
- **«Перевод (LLM)»: «Повторы» в экспертной форме** — перезапуск
  упавшей стадии с повторами достижим из web (раньше — только в CLI).
- **Запуски: панель зависала после остановки** — остановленный запуск
  продолжал показываться «Активным» со статусом running, а «Показать»
  открывал прежнюю страницу; теперь панель, статус и лог обновляются
  сразу по событию SSE.
- **В логах перестал маскироваться `--max_tokens`** — значение
  подменялось на «••••» вместе с настоящими секретами стартовой
  команды.
- **«Проверка»: старое значение в парах правок — серым без
  зачёркивания** (вместо line-through) — читается легче.
- **Автоудаление дубля заголовка — защита от ложных срабатываний**:
  допуск декорации стал пропорционален длине заголовка (короткие
  «I»/«1» больше не поедают «I. Начало»), промо-хвосты вроде
  【求全订，求月票】 снимаются до сравнения, сравнение по NFC.

### Сборка и публикация

- **docker-compose ссылается на готовый образ**
  `ghcr.io/lynxpda/novelmaestro:latest` (публикуется автоматически
  при пуше тега). Обновление готового образа:
  `docker compose pull && docker compose up -d`; локальная сборка —
  `docker compose up -d --build`.

## 0.1.7

### Исправления

- **Список проектов не обновлялся после удаления книги**: модалка
  закрывалась, но список и статистика хаба оставались прежними до
  перезагрузки страницы. Теперь кеш статистики точечно
  инвалидируется при создании/переносе/переименовании/копировании/
  удалении, список перерисовывается сразу.
- **Загрузка обложек `cover1.png`, `cover2.png` …**: интерфейс
  сообщал об успехе, но реально сохранялся только `cover.png`.
  Загрузка обложки теперь кладёт файл в `source/` под исходным
  именем (с подтверждением перезаписи), удаление — выбранного
  файла, а не всегда `cover.png`.
- **Переименование файла открывало пустое поле**: теперь поле
  предзаполнено текущим именем с выделением — достаточно поправить
  и подтвердить.
- **Regexp-замены почти не работали в epub и массовых заменах**:
  правила компилировались без MULTILINE — `^`/`$` матчили весь
  текст, а не строки; пробелы по краям паттерна и замены молча
  обрезались (чисто пробельная замена = удаление пропадала); в
  toc-режиме разбивки замены вообще не применялись к заголовкам и
  телу; очистки в web-форме проходили через обрезку краёв. Теперь:
  MULTILINE+UNICODE во всех режимах, значимые пробелы, единые
  флаги `|i` (регистр) и `|r` в конце правила во всех точках
  (файл правил, `--replace`, web-поля), предпросмотр применяет те же
  замены, что и реальный запуск. Примеры очистки отступов и
  дубликатов строк добавлены в справки и шаблон правил.
- **Дубль названия главы в начале тела**: epub повторял заголовок
  первым абзацем (с отступом U+2003) — в toc-режиме дубль оставался
  в тексте, в regexp-режиме удалялся лишь случайно. Теперь все
  ведущие копии заголовка снимаются автоматически в обоих режимах
  (пустые строки между копиями пропускаются), с защитой от прозы:
  длинная строка, содержащая заголовок, сохраняется.

### Тесты

- 989 тестов (добавлены: значимые пробелы и MULTILINE-якоря правил,
  флаги `|i`/`|r`, NFC-нормализация, дубли заголовков, точечная
  инвалидация статистики).

## 0.1.6

### Исправления

- **В Docker LLM-конфиг не доходил до стадий**: в образе файла `.env`
  нет, compose пробрасывает его переменными окружения (`env_file`),
  но `get_server_config`/`get_stage_model` читали только файл —
  стадии получали пустой или «примерный» хост. Теперь приоритет
  `os.environ` > файл (канон §7), и `HOST`/`API_KEY`/`MODEL`,
  а также стадийные `<СТАДИЯ>_*` работают из окружения.
- **Загрузка файлов целиком читалась в память**: multipart-тело
  буферизовалось до парсинга (обход лимита размера). Переписано на
  потоковый разбор с spool-временными файлами и атомарной записью;
  при 400/413 на диск не пишется ничего, имена файлов обрезаются до
  basename, вложенная загрузка в `chapters/...` разрешена.
- **Перезапись файлов при загрузке — без спроса**: если файл в проекте
  уже есть, ранее он молча заменялся. Теперь — модалка с явным
  подтверждением.
- **Гонка в операциях с проектами**: перенос/удаление/переименование
  параллельно (многопоточный web-сервер) могли перемешать состояние
  `.sections.json`; операции с папками проектов — под мьютексом.
- **`copy_project` копировал `.env`**: секреты утекали в копию
  проекта. Теперь `.env` в копиях игнорируется.
- **В Docker терялись наборы шаблонов**: вкладка «Шаблоны» жила
  внутри контейнера и пропадала при пересоздании. Добавлен монтаж
  `./templates:/app/templates`.

### Документация

- `packaging/README.md` → «Обновление»: что переживает обновление
  Docker (projects/templates/job_logs/токен), что нет, вариант
  обновления готового образа через `docker compose pull`.
- Прописано пробрасывание `.env` в Docker (env_file → окружение,
  правки на хосте + `docker compose up -d`).
- Предупреждение: Docker + `WEB_HOST=0.0.0.0` без `WEB_AUTH=1`
  открывает интерфейс всей LAN.

## 0.1.5

### Исправления

- **Дубли и призраки разделов в «Проектах»**: параллельные запросы
  создания раздела (двойной клик по «ОК»; сервер многопоточный)
  проходили проверку «уже существует» вместе и писали раздел в
  `.sections.json` дважды — в списке он появлялся x2, а запись без
  папки на диске оставалась навсегда. Операции со списком разделов
  теперь под мьютексом, `load_sections` схлопывает дубли и
  самолечится (записи без папки игнорируются), кнопка «ОК» модалки
  блокируется на время запроса.
- **Пустые «Раздел» и «Шаблон» при создании проекта**: модалка читала
  разделы/шаблоны из кеша хаба, который сбрасывается операциями с
  разделами — при `null` селекты оставались пустыми, создать проект
  было нельзя. Теперь данные загружаются заново при открытии модалки
  (с сообщением об ошибке, если сервер недоступен).
- **Хаб не обновлялся после изменения разделов**: создание/
  переименование/удаление раздела перерисовывало только модалку,
  страница «Проекты» за ней оставалась старой до перезагрузки.

## 0.1.4

### Исправления

- **Вкладки «Глоссарий» и «Проверка» пустые**: глобал `PAGE_SIZE`
  (размер страницы таблиц) пропал из `app.js` при рефакторинге —
  вкладки падали с `ReferenceError`. Определён локально в
  `project-views.js`, где используется.
- **Новый проект не появлялся до обновления страницы**: `close()`
  модалки создания резолвил промис `false` ещё до окончания загрузок,
  поэтому хаб не перерисовывался. Теперь `close(true)` — сразу после
  создания, опциональные обложка/исходник догружаются фоном.
- **Загрузка исходника в мастере создания**: `POST /api/upload` без
  параметра `project` — файл не загружался; добавлен `?project=…`.

### Сервер

- Адрес по умолчанию — **`127.0.0.1`** (только этот компьютер); для
  доступа с LAN — `--host 0.0.0.0` (тогда рекомендуется `--auth`).
- Порт занят → сервер автоматически берёт следующий свободный
  (`port+1 … port+100`) и показывает фактический адрес в консоли.

### Промпты

- translate/polish поддерживают тег `{original_text}` (как redact);
  тег обязателен — нет тега: предупреждение в лог (один раз на режим),
  текст дописывается после промпта (обратная совместимость убрана).

### Сборка Windows

- `README.txt` переименован в `START.txt` (краткая инструкция по
  запуску; основная документация — `README.md`).

## 0.1.1

Исправления и полировка после первого релиза.

### Исправления

- **Безопасность web-сервера**: отрицательный `Content-Length` больше не
  вешает поток сервера (`read(-1)` ждал EOF) — теперь мгновенный 400;
  раздача статики проверяет путь через `is_relative_to`, а не префиксную
  `startswith` — файл-«сосед» вида `static_evil.txt` больше не отдаётся
  через `GET /../static_evil.txt` (raw-запросы).
- **CI**: к GitHub Release прикрепляется только портативный Windows-пакет
  (zip), без дублирующих артефактов.

### Шаблоны

- NER-промпт: поле `pinyin` заменено на универсальное `reading`
  (произношение), скрипт принимает оба имени поля.
- Отдельные промпты стадий (`translate_prompt.txt`, `redact_prompt.txt`,
  `polish_prompt.txt`) удалены — конвейер использует единый
  `pipeline_prompt.txt` с тегами `<translate>/<redact>/<polish>`.
- Шаблонный `replacements.txt` убран из набора General (правила массовых
  замен — только корневой `replacements.txt.example`).
- `donate.txt` обезличен (личные ссылки заменены плейсхолдерами); единый
  источник — `templates/General/source/donate.txt`.

### Документация

- `help.md` и `DEVELOPERS.md`: раздел «Нюансы промптов» (теги vs файл
  целиком, NER-поля, единый pipeline_prompt.txt), универсальность
  перевода «с любого языка на русский», лог-файл NER.
- `TODO.md` исключён из сборок (docker/robocopy), добавлена инструкция
  по публикации релизов (`packaging/README.md`).
