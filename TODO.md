# TODO — бэклог

> Бэклог не реализуется без отдельной команды пользователя.
> Статусы обновляются по мере выполнения; коммиты — по каждой
> законченной задаче, а не в конце сессии.

## Текущая сессия (0.1.12 — после релиза 0.1.11)

- [x] **0. Релиз 0.1.11** — зафиксировать текущее состояние
      (пороги count, валидация потоков, docker .env, localStorage
      тема, «Спорные»): CHANGELOG, тег `v0.1.11`, пуш (Actions
      соберут образ и Windows-сборку).
- [x] **1. ner_check · потоки** — `--threads` (1–16), все батчи
      всех проходов в общий пул, общий прогрессбар; коммит 9bf9fda.
- [x] **2. wiki · универсальные настройки** — теги <wiki_markers>/
      <wiki_type_names_ru>/<wiki_relations_labels>/<wiki_skip_relations>/
      <wiki_type_order> в wiki_prompt.txt; коммит 3c00717.
- [x] **3. ner_check · RAG-режим (FTS5)** — `--passes rag`,
      FTS5-утилиты в core.common, тег <prompt_rag>,
      равномерная выборка even_sample, бюджет rag_budget;
      отличия → патчи stage=RAG; коммит 9836618.
- [x] **4. ner.json · опциональные поля** — count опционален:
      порог 0 выключает фильтр (записи без count не выпадают),
      строки нормализуются _int_count во всех сортировках;
      коммит 2c837e7.
- [x] **5. translate_check · настройки** — коэффициенты пресетов
      (--ratio-*/--tol-*, эвристики), отключаемые проверки
      не-русских символов и последовательности глав + свои regexp;
      коммит 8a1763e.
- [x] **6. compile · очистка текста** — --no-clean / --clean-regex
      (удаление «Глава N» из тела, default вкл); коммит 82e1282.
- [x] **7. БАГ: пустая вкладка «Глоссарий»** — ReferenceError:
      dispute/voteKeys/disputeVictims/saveDispute не были объявлены;
      добавлены объявления + localStorage-состояние + регрессионный
      тест (коммит f2c298e).

## Текущая сессия (0.1.16 — translate_check простой/экспертный, комментарии в regexp, светлый предпросмотр)

- [x] **1. AGENTS.md** — правило о комментариях в коде: минимальные,
      только для понимания, без номеров раундов/этапов (не дневник).
- [x] **2. translate_check · Simple/Expert** — «Тип файлов глав» вместо
      пресета; ratio+tol в одном поле «ratio±tol» (Стадия/Стадия и
      Оригинал/Стадия); чекбоксы/свои форматы убраны — универсальные
      regexp-проверки (multiline, всё найденное — ошибка, заголовок
      главы не считается); простой режим с пресетом «Проверить перевод».
- [x] **3. Комментарии « # …» в regexp-полях** (epub, translate_check,
      batch_replace) + автозаполнение из .env одной строкой с «\n».
- [x] **4. Светлый предпросмотр** — явный color: var(--text) у
      br-text/epub-prev-text/compile-prev-text (чёрные буквы на
      тёмном фоне больше невозможны при любой теме).

## Текущая сессия (0.1.17 — оценка перевода LLM, типы файлов глав)

- [x] **1. Стадия «Оценка перевода (LLM)»** (`translate_quality`, между
      batch_replace и compile): один LLM-запрос по пакету глав —
      `{original_text}` (chapter.txt) + `{translated_text}` (тип файлов
      глав) в промпте (тег `<prompt_assessment>`,
      `templates/General/prompts/translate_quality_prompt.txt`); простой
      режим (start/end/type/prompt_file), экспертный — + LLM-поля и
      бюджет СИМВОЛЫ (главы+промпт; не влезает — обрезка до целого
      количества глав); md-отчёт с технической шапкой (дефолт
      translation_quality_assessment.md); секция «Проверки» →
      «Оценка перевода (LLM)» с выбором md-файла и рендером.
- [x] **2. compile · «Тип файлов глав»** — поле source_type
      переименовано (было «Исходный файл главы»).

### Правки после тестов (0.1.17)

- [x] **3. Вкладка «Проверки» падала** — renderQualityReports
      глобальная, использовала section/name из замыкания
      (ReferenceError); параметры у обоих рендереров + node-тест
      tabs.test.mjs (все вкладки с DOM-mock).
- [x] **4. Простой режим: дубль диапазона** — start/end не рисуются
      отдельно (единая строка «Главы: [start] – [end]»).
- [x] **5. wiki «Источник текста»** — дефолт «Собрать из глав».
- [x] **6. Кнопки «+»/«x»** — btn-ghost, как остальные.

## Текущая сессия (0.2.0 — CHANGELOG без дат, диапазон по бюджету, RAG-поля, релиз)

- [x] **1. CHANGELOG.md без дат** — заголовки `## <версия>`; правило
      в AGENTS.md §9: релиз только по запросу пользователя, в
      changelog — только скоуп релиза. Незарелиженные 0.1.13–0.1.17
      собраны в `## 0.2.0` (расхождение с тегом v0.1.12 устранено).
- [x] **2. «Оценка перевода»: конечная глава по бюджету** — в форме
      только начало диапазона; end рассчитывается интерактивно
      (бюджет − промпт, главы = перевод+оригинал, не более последней
      главы); budget в простом режиме; node-тест quality.test.mjs.
- [x] **3. ner_check RAG: поля записи в промпте** — чипсы «Поля в
      запросе LLM» (термин всегда) уходят в RAG-промпт с фрагментами;
      чипсы полей видны в RAG-режиме, типы — скрыты; fields в params.
- [x] **4. Справка актуализирована** — translate_quality, RAG-поля,
      единый диапазон, «Тип файлов глав», wiki-дефолт, вкладка
      «Проверки».
- [x] **5. Релиз 0.2.0** — тег `v0.2.0` + пуш (Actions: Docker +
      Windows); GitHub Release создаёт windows.yml.

## Текущая сессия (0.2.1 — баги 0.2.0, RAG по term, дефолты translate_check, релиз)

- [x] **1. БАГ: отчёт «Оценка перевода» не показывался** — внутренняя
      renderFile затеняла имя проекта (404 в /api/file), ошибка не
      вставлялась в wrap; параметр переименован в fname, err в wrap.
- [x] **2. translate_quality: Экспертный режим** — end редактируемый,
      ручной ввод проверяется по бюджету (clamp до последней влезающей
      главы + подсказка), простой режим — авторасчёт как раньше;
      починено чтение budget в экспертной форме (поле .field).
- [x] **3. translate_quality промпт** — {original_text}/{translated_text}
      вставлены в тело тега <prompt_assessment> (были только в
      комментариях — промпт не подставлял текст).
- [x] **4. translate_check форма** — типы без уточнений в скобках,
      убран чекбокс «Мягкий режим (--lenient)» (CLI сохраняет флаг),
      regexp/neighbor/original предзаполнены дефолтами скрипта,
      раскрывающаяся справка по regexp.
- [x] **5. ner_check RAG: поиск по term** — FTS5 искал по translation;
      теперь по term (с fallback на translation); корень проблемы —
      unicode61 не токенизирует сплошной CJK: иероглифы разбиваются
      пробелами в индексе (searchable-колонка), выдача — оригинал;
      в Экспертном режиме скрыты batch_size/count_threshold/threads
      (не используются в run_rag).
- [x] **6. «Проверка перевода»: кнопка «Очистить»** — подтверждение
      «УДАЛИТЬ», DELETE /api/file по logs/check_*.txt, пустое
      состояние после очистки.
- [x] **7. merge_fix_entries** — проверено: дедуп по chapter+old+new,
      повторный кандидат не пишется, статус/правки человека не
      затираются.
- [x] **8. Тесты** — 1056 pytest + 78 node-тестов зелёные; новые:
      RAG-поиск по term (китайский текст), build без --lenient.
- [x] **9. Релиз 0.2.1** — CHANGELOG, тег v0.2.1, пуш, Release.

## Текущая сессия (0.1.15 — RAG-спорные, редактор промптов, docker-история)

- [x] **1. ner_check · RAG** — чипсы типов/полей скрываются в RAG
      (не влияют); «Добавить спорные»: баг — `_votes_*` это ОБЪЕКТ
      `{вариант: голоса}`, а код делал `v.map(Number)` → TypeError,
      термины не добавлялись; теперь Object.values + счётчики по
      типам «Person (male) (2)» пересчитываются живьём при смене
      коэффициента/поля/порога; «Коэффициент:» default 1.5;
      rag_budget default 65536 (CLI + web).
- [x] **2. Промпты в запусках** — промпт не выбран → «Загрузить»;
      выбран → «Редактировать» (модалка с CodeMirror, чтение/правка/
      сохранение через /api/prompts/{name}).
- [x] **3. docker · история запусков** — jobs.json лежал в web/,
      а volume монтировал только web/job_logs → при пересоздании
      контейнера история терялась (логи оставались). jobs.json
      перенесён в job_logs/jobs.json (внутрь volume) + миграция
      старого файла + исключение jobs.json из cleanup-сирот +
      mkdir перед persist.

## Текущая сессия (0.1.14 — правки после тестов)

- [x] **1. Глоссарий · меню** — меню «+»/«x» белые с белыми
      надписями в тёмной теме: `--card-bg` не определена → fallback
      `#fff`; заменено на `--surface` + явный `color: var(--text)`;
      «+ Термин» не показывал запись: при сортировке count ↓
      новая запись (count=null) уходила из видимого slice — сброс
      поиска/фильтра + докидка редактируемой записи в renderRows.
- [x] **2. ner_check · «Добавить спорные»** — выбор по полю
      голосов `_votes_xxx` (select «Все поля голосов» + поля),
      типы — чипсы с «Выбрать все/Снять все» (минимум один);
      «Коэффициент (max/2-й):» → «Коэффициент:» default 1.5.
- [x] **3. ner_check · RAG-промпт** — «RAG: промпт-файл» убран
      (дубль): RAG-промпт живёт в общем «Промпт-файле» (тег
      `<prompt_rag>`, CLI: `--rag_prompt_file or --prompt_file`);
      автоподхват ner_check_prompt.txt перенесён на prompt_file.
- [x] **4. compile · очистка убрана** — поля «Отключить очистку
      текста»/«Свой regexp очистки» и удаление «Глава N» в теле
      удалены из compile (дубль batch_replace); справка про
      удаление лишних заголовков `^Глава\s+(\d+|\[Номер\])\s*$ ->`
      добавлена в «Массовые замены».

## Текущая сессия (0.1.13 — правки списка)

- [x] **1. Глоссарий · мини-панельки** — баг: `.menu-box[hidden]`
      перекрывался `display: flex` — панельки видны всегда;
      добавлено правило `.menu-box[hidden] { display: none; }`.
- [x] **2. Глоссарий · убрать «Спорные»** — dispute-код выпилен
      из project-views.js; «Добавить спорные» перенесена в Запуски
      ner_check (RAG-режим).
- [x] **3. ner_check · режимы** — переименования («по отдельности»,
      «Порог count»); RAG-поля видны только в RAG-режиме;
      автоподхват ner_check_prompt.txt; убраны auto-apply/no-bak;
      потоки — единый пул max_workers (проверено, лимит держится);
      «RAG: файл книги» → диапазон глав + тип файла (сборка
      в памяти, --rag_source_type); кнопка «Добавить спорные».
- [x] **4. Светлая тема · подсветка кода** — `--bg-soft` не была
      определена ни в одной теме (fallback #2a2a2a — тёмный фон
      с тёмным текстом); переменная добавлена в обе темы.
- [x] **5. Редактор · n-грамма/порог** — жёсткое LCS >= 0.8 длины
      не зависело от порога: «нити» (3/4 = 0.75) не находилась;
      теперь общая подстрока меряется по порогу (ui-core +
      core.common._fuzzy_hit) + регрессионный тест.
- [x] **6. Шаблоны · просмотр файлов** — General: «Просмотр»
      (read-only, `setReadOnly`), «Сохранить» не рендерится.
- [x] **7. wiki · UI** — «Сохранить как главу»; text-поля
      exclude_types/types → чипсы типов из глоссария (все выбраны).
- [x] **8. translate_check · слова-исключения** — дефолт
      VIP,MVP,【,】,NPC убран (пусто = ничего); поле формы
      заполняется из TRANSLATE_CHECK_EXCLUDE_WORDS только если
      ключ задан.

## Бэклог (не в текущей сессии)

- [ ] **UI-проб**: headless-браузер (Playwright) + `tools/ui_probe.py`
      — обход SPA со скриншотами, console-ошибки, падение при
      ошибках; `tools/seed_demo.py` — демо-проект. Ограничение:
      системный Python PEP 668 (apt/node-playwright или venv —
      решить с пользователем).
