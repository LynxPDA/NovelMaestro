# TODO — Справка в web-интерфейсе и разделение документации

Статус: **готово**. Вкладка «Справка» (левая панель) — полноценный
раздел помощи; README.md — лендинг для пользователей, технические
подробности — в DEVELOPERS.md.

## Техническое задание

1. **Вкладка «Справка».** Заполнить заглушку `viewHelp()` (app.js):
   * О программе — общая информация о NovelMaestro;
   * Управление проектами — создание и настройка проектов;
   * Работа с проектом — общие принципы + подробное описание каждой
     вкладки;
   * Шаблоны — руководство по использованию;
   * Запуски — общая информация + инструкции по каждому виду запуска;
   * Регулярные выражения (RegExp) — применяемый диалект, базовые
     основы, обратные ссылки, примеры с разбором.
2. **Разделение документации.** README.md → лендинг для простых
   пользователей; техническая документация для разработчиков — в
   отдельном файле `DEVELOPERS.md`.

## Исследование (факты, на которых строится план)

* **Справка.** `viewHelp()` — заглушка в `app.js` (строки ~2376):
  «Раздел в разработке». Вендор `marked v12` уже подключён
  (`window.marked.parse`, `{mangle:false, headerIds:false}`);
  заголовки id НЕ получают — TOC строим сами, обходя DOM.
  Статика отдаётся из `web/static/` любым путём (`_serve_static`) —
  контент справки можно положить отдельным `help.md` и грузить
  fetch'ем без правок сервера. Паттерн страницы: `.page` +
  `.page-header` + тело; стили md-предпросмотра есть только для
  iframe (`previewCss`) — для инлайнового рендера нужны свои
  `.md-body`-стили.
* **README.** Сейчас 598 строк, смешаны пользовательские и
  технические разделы. Тесты: `test_docs.py::test_no_legacy_launcher_names`
  проверяет README.md на устаревшие имена; `test_architecture.py`
  требует `web/README.md`. AGENTS.md §1 называет README.md
  «первоисточником» — обновить упоминание на «README.md (лендинг) +
  DEVELOPERS.md (технические подробности)».

## План

### 1. Справка (web/static)

* [x] `web/static/help.md`: контент справки (markdown, русский):
  О программе, Управление проектами, Работа с проектом (общие
  принципы + каждая вкладка), Шаблоны, Запуски (общая информация +
  инструкции по каждой стадии), Регулярные выражения (диалект Python
  re, основы, обратные ссылки, примеры с разбором).
* [x] `app.js` `viewHelp()`: fetch `help.md` → `marked.parse` →
  рендер в `.md-body`; TOC по h2/h3 из готового DOM (свои id);
  слева — липкий список разделов, клик — плавный скролл; ошибка
  загрузки — понятное сообщение.
* [x] `styles.css`: `.help-layout` (двухколоночный), `.help-toc`
  (липкий), `.md-body` (типографика: заголовки, код, таблицы,
  blockquote, ul/ol, hr).
* [x] Проверка: `node --check app.js`; сервер отдаёт /help.md,
  вкладка рендерится (6 H2, TOC по h2/h3).

### 2. Разделение документации

* [x] `DEVELOPERS.md`: техническая документация — архитектура
  (core/cli/web), конвейер и стадии, структура проекта, конфигурация
  LLM-сервера (приоритеты, ключи по стадиям), промпты (теги,
  плейсхолдеры), шаблоны, тесты, соглашения (единицы, Unicode, JSON),
  ссылки на web/README.md и AGENTS.md.
* [x] `README.md` → лендинг: что это, возможности, быстрый старт,
  первые шаги (создание проекта, запуски), краткий обзор вкладок,
  конфигурация для пользователя, ссылки на Справку (в приложении),
  DEVELOPERS.md, AGENTS.md.
* [x] `AGENTS.md` §1: README.md — лендинг, DEVELOPERS.md — технические
  подробности.
* [x] Проверка: `python3 -m pytest tests/ -q` зелёные (939).

### 3. Доки и коммит

* [x] `web/README.md`: упомянута вкладка «Справка» (help.md).
* [x] Smoke: сервер отдаёт /help.md, /api/session — 200.
* [x] Коммит+пуш по стандарту Conventional Commits.
